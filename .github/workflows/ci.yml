name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: stluser
          POSTGRES_PASSWORD: stlpass
          POSTGRES_DB: stldb
        options: >-
          --health-cmd "pg_isready -U stluser -d stldb"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

      kafka:
        image: apache/kafka:4.0.1
        options: >-
          --env KAFKA_KRAFT_MODE=true
          --env KAFKA_LISTENERS=PLAINTEXT://:9092
          --env KAFKA_ADVERTISED_LISTENERS=PLAINTEXT://kafka:9092

    steps:
      - uses: actions/checkout@v3

      - name: Set up JDK 21
        uses: actions/setup-java@v3
        with:
          java-version: 21
          distribution: temurin

      - name: Wait for Postgres
        run: |
          echo "Waiting for Postgres..."
          timeout 60 bash -c 'until pg_isready -h postgres -U stluser -d stldb; do sleep 5; done'

      - name: Wait for Kafka
        run: |
          echo "Waiting for Kafka..."
          timeout 60 bash -c 'until nc -z kafka 9092; do sleep 5; done'

      - name: Build with Maven
        run: ./mvnw clean package

      - name: Run tests
        run: ./mvnw test

